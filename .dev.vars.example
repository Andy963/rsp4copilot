#
# Copy to `.dev.vars` for local development:
#   cp .dev.vars.example .dev.vars
#
# Wrangler will load these when you run `wrangler dev`.
#
# NOTE: `.dev.vars` is ignored by git. Do NOT commit real secrets.
#
# NOTE: This project is designed for relay servers (e.g. Claude Relay Server),
#       NOT for direct official API connections.
#

# Upstream (Responses API) - use your relay server URL
# OPENAI_BASE_URL=https://your-relay-server.example/openai
# RESP_RESPONSES_PATH=/v1/responses
# RESP_REASONING_EFFORT=low
# # (default is low; set to "off" to not send reasoning params)

# Optional: Gemini upstream - use your relay server URL
# GEMINI_BASE_URL=https://your-relay-server.example/gemini
# GEMINI_API_KEY=...
# GEMINI_DEFAULT_MODEL=gemini-3-pro-preview

# Optional: Claude upstream - use your relay server URL
# CLAUDE_BASE_URL=https://your-relay-server.example/api
# CLAUDE_API_KEY=...
# CLAUDE_DEFAULT_MODEL=claude-sonnet-4-5-20250929
# CLAUDE_MESSAGES_PATH=/messages
# CLAUDE_MAX_TOKENS=8192

# Debug logging
# RSP4COPILOT_DEBUG=true

# Chat history limits (optional; defaults shown; set 0 to disable)
# RSP4COPILOT_MAX_TURNS=12
# RSP4COPILOT_MAX_MESSAGES=40
# RSP4COPILOT_MAX_INPUT_CHARS=300000

# For local dev only you can put secrets here (Cloudflare recommends using `wrangler secret put` for production):
# OPENAI_API_KEY=sk-...
# WORKER_AUTH_KEY=your-worker-auth-key
# Optional: multiple client auth keys (comma-separated)
# WORKER_AUTH_KEYS=key1,key2,key3

# Model listing for /v1/models
DEFAULT_MODEL=gpt-5.2
ADAPTER_MODELS=gpt-5.1,gpt-5.1-codex-max,gpt-5.2,gpt-5.2-codex,gemini-3-flash-preview,gemini-3-pro-preview,claude-haiku-4-5-20251001,claude-sonnet-4-5-20250929,claude-opus-4-5-20251101
