{
  "name": "cf-worker",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --keep-vars",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "rm -rf dist/test && tsc -p tsconfig.test.json && printf '{\"type\":\"commonjs\"}\\n' > dist/test/package.json && node --test test/**/*.test.cjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251228.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.56.0"
  }
}
